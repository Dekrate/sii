spring:
    application:
        name: sii
    docker:
        compose:
            enabled: false

    h2:
        console:
            enabled: true
    liquibase:
        change-log: db/changelog/db.changelog-master.xml
server:
    http2:
        enabled: true
